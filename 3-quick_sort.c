#include "sort.h"

/**
 * quick_sort - sorts an array in ascending order
 * @array: the array to be sorted
 * @size: the size of the array
 * Return: void
 */

void quick_sort(int *array, size_t size)
{
	int tmp = 0, pivot_index = 0, pivot_value           
	size_t j = 0, i = -1;

	for (j = 0; j < size; j++)
	{
		if (array[j] < 
	}
}
